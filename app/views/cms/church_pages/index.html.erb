<div class='top_right_controls'>
  <%= link_to 'New page', main_app.new_cms_church_page_path(@church), :class => 'btn btn-info' %>
</div>

<div class="page_header">
	<h1>Pages</h1>
</div>

<section class="page_content">
	<% @pages.each do |page| %>
    <div class="index_item">
			<div class="controls">
				<%= link_to 'Delete', main_app.church_page_path(@church, page), :method => :delete, :confirm => 'Are you sure?', :class => 'btn btn-danger' %>							
				<%= link_to "Edit", main_app.edit_cms_church_page_path(@church, page), :class => 'btn btn-warning' %>				
				<%= link_to 'View', host_page_path(page), :class => 'btn' %>
			</div>
			<%= link_to page.name, main_app.edit_cms_church_page_path(@church, page) %>
		</div>
	<% end %>
</section>